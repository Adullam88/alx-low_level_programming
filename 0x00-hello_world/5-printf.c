#include <stdio.h>

/**
 * main - prints the below output
 *
 * Return: Always 0 if successful, else 1
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
