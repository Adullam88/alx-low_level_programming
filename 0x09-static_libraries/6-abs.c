#include "main.h"

/**
 * _abs - computes the absolute value of the number
 * @n: The number to find absolute value for
 *
 * Return: 0 means success
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
